# Normalize all text files to LF inside the repository,
# regardless of the OS used to clone it.
* text=auto eol=lf

# Windows-specific scripts must keep CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Shell scripts must keep LF
*.sh  text eol=lf
mvnw  text eol=lf
