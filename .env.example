# Environment variables template.
# Copy it to .env and fill in real values:
#   PowerShell:  Copy-Item .env.example .env
# The real .env must NOT be versioned (already in .gitignore).

DB_NAME=rides_db
DB_USER=rides_app
DB_PASSWORD=set_a_strong_password_here
